<mvc:View
    controllerName="z.sap.courses.controller.TrainingAssignmentsList"
    xmlns="sap.m"
    xmlns:mvc="sap.ui.core.mvc"
    xmlns:core="sap.ui.core"
    xmlns:smartFilterBar="sap.ui.comp.smartfilterbar"
    xmlns:smartTable="sap.ui.comp.smarttable">

    <Page
        id="assignmentsListPage"
        showNavButton="true"
        navButtonPress="onNavBack"
        class="sapUiResponsivePadding--header sapUiResponsivePadding--content">

        <customHeader>
            <Bar id="assignHeaderBar">
                <contentLeft>
                    <Button id="navBackBtn" icon="sap-icon://nav-back" press="onNavBack" type="Back" />
                    <Title id="assignPageTitle" text="Training Assignments" level="H2" />
                </contentLeft>
                <contentRight>
                    <Button
                        id="backToCatalogBtn"
                        text="Back to Catalog"
                        icon="sap-icon://learning-assistant"
                        press="onNavBack" />
                </contentRight>
            </Bar>
        </customHeader>

        <content>
            <!-- My Progress Analytics Panel -->
            <Panel
                id="myProgressPanel"
                expandable="true"
                expanded="true"
                headerText="My Learning Progress"
                class="sapUiSmallMarginBottom">
                <HBox id="myProgressHBox" justifyContent="SpaceAround" wrap="Wrap" class="sapUiSmallMargin">
                    <VBox id="myAssignedBox" alignItems="Center" class="sapUiSmallMarginEnd">
                        <ObjectNumber
                            id="myAssignedCount"
                            number="{assignAnalytics>/assigned}"
                            unit="Pending"
                            state="Warning" />
                        <Text id="myAssignedLabel" text="To Start" class="sapUiTinyMarginTop" />
                    </VBox>
                    <VBox id="myInProgressBox" alignItems="Center" class="sapUiSmallMarginEnd">
                        <ObjectNumber
                            id="myInProgressCount"
                            number="{assignAnalytics>/inProgress}"
                            unit="Learning"
                            state="Warning" />
                        <Text id="myInProgressLabel" text="In Progress" class="sapUiTinyMarginTop" />
                    </VBox>
                    <VBox id="myCompletedBox" alignItems="Center" class="sapUiSmallMarginEnd">
                        <ObjectNumber
                            id="myCompletedCount"
                            number="{assignAnalytics>/completed}"
                            unit="Done"
                            state="Success" />
                        <Text id="myCompletedLabel" text="Completed" class="sapUiTinyMarginTop" />
                    </VBox>
                    <VBox id="myProgressBox" alignItems="Center">
                        <ProgressIndicator
                            id="myProgress"
                            percentValue="{assignAnalytics>/completionPercent}"
                            displayValue="{assignAnalytics>/completionPercent}%"
                            state="{= ${assignAnalytics>/completionPercent} >= 80 ? 'Success' : ${assignAnalytics>/completionPercent} >= 50 ? 'Warning' : 'Error' }"
                            width="150px" />
                        <Text id="myProgressLabel" text="My Progress" class="sapUiTinyMarginTop" />
                    </VBox>
                </HBox>
            </Panel>

            <!-- SmartFilterBar for Assignments - expanded with filter boxes -->
            <smartFilterBar:SmartFilterBar
                id="assignSmartFilterBar"
                entitySet="TrainingAssignments"
                persistencyKey="AssignmentsSmartFilter"
                enableBasicSearch="true"
                showClearOnFB="true"
                filterBarExpanded="true"
                showFilterConfiguration="true" />

            <!-- SmartTable for Assignments -->
            <smartTable:SmartTable
                id="assignSmartTable"
                entitySet="TrainingAssignments"
                smartFilterId="assignSmartFilterBar"
                tableType="ResponsiveTable"
                header="Assignments"
                showRowCount="true"
                enableAutoBinding="true"
                showFullScreenButton="true"
                enableExport="true"
                useVariantManagement="true"
                useTablePersonalisation="true"
                persistencyKey="AssignmentsSmartTable"
                demandPopin="true"
                enableAutoColumnWidth="true"
                initialise="onSmartTableInit">

                <smartTable:customToolbar>
                    <OverflowToolbar id="assignTableToolbar">
                        <ToolbarSpacer id="assignToolbarSpacer" />
                        <Button
                            id="assignRefreshBtn"
                            icon="sap-icon://refresh"
                            tooltip="Refresh Data"
                            press="onRefresh" />
                    </OverflowToolbar>
                </smartTable:customToolbar>
            </smartTable:SmartTable>
        </content>
    </Page>
</mvc:View>
