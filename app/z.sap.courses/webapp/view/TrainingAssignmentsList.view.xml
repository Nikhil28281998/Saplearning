<mvc:View
    controllerName="z.sap.courses.controller.TrainingAssignmentsList"
    xmlns="sap.m"
    xmlns:mvc="sap.ui.core.mvc"
    xmlns:core="sap.ui.core"
    xmlns:smartFilterBar="sap.ui.comp.smartfilterbar"
    xmlns:filterBar="sap.ui.comp.filterbar"
    xmlns:smartTable="sap.ui.comp.smarttable"
    xmlns:f="sap.f"
    xmlns:cssgrid="sap.ui.layout.cssgrid">

    <Page
        id="assignmentsListPage"
        title=""
        showNavButton="false"
        enableScrolling="false"
        class="assignmentsPage">

        <headerContent>
        </headerContent>

        <dependents>
            <core:InvisibleText id="assignmentsFilterLabel" text="{i18n>assignmentsFilterLabel}" />
            <core:InvisibleText id="assignmentsTableLabel" text="{i18n>assignmentsTableLabel}" />
            <core:InvisibleText id="assignRefreshLabel" text="{i18n>refreshData}" />
        </dependents>

        <content>
            <!-- ============================================================ -->
            <!-- U-3: Due Date Warning Banner                                  -->
            <!-- ============================================================ -->
            <HBox id="dueDateWarningBanner"
                  class="dueDateWarningBar sapUiSmallMarginBottom sapUiSmallMarginBeginEnd"
                  alignItems="Center"
                  visible="{= ${assignAnalytics>/dueSoonCount} > 0 }">
                <core:Icon id="dueDateWarningIcon" src="sap-icon://warning2" size="1.25rem" color="#e76500" class="sapUiTinyMarginEnd" />
                <Text id="dueDateWarningText"
                      text="{= ${assignAnalytics>/dueSoonCount} === 1 ? '1 assignment is due within 3 days!' : ${assignAnalytics>/dueSoonCount} + ' assignments are due within 3 days!'}"
                      class="sapUiTinyMarginEnd" />
                <Button id="dueDateWarningBtn" text="{i18n>viewDueSoon}" type="Transparent" press="onFilterDueSoon" />
            </HBox>

            <!-- ============================================================ -->
            <!-- MY PROGRESS – User's own assignment stats (always visible)   -->
            <!-- ============================================================ -->
            <Panel
                id="myProgressPanel"
                expandable="true"
                expanded="true"
                headerText="{i18n>myProgress}"
                class="sapUiSmallMarginBottom analyticsPanel">

                <!-- 5 KPI Cards: Total + Assigned + In Progress + Overdue + Completed (with % merged) -->
                <VBox id="myProgressHBox" class="analyticsContainer userAnalyticsContainer">
                    <VBox id="myTotalBox" alignItems="Center" class="analyticsCard analyticsCardBlue analyticsCardClickable" tooltip="{i18n>clickToFilter}">
                        <core:Icon id="myTotalIcon" src="sap-icon://group" size="1.5rem" color="#0854a0" class="sapUiTinyMarginBottom" />
                        <ObjectNumber
                            id="myTotalCount"
                            number="{assignAnalytics>/total}"
                            state="Information"
                            class="analyticsNumber" />
                        <Text id="myTotalLabel" text="{i18n>myTotalAssignments}" class="analyticsLabel" />
                    </VBox>

                    <VBox id="myAssignedBox" alignItems="Center" class="analyticsCard analyticsCardOrange analyticsCardClickable" tooltip="{i18n>clickToFilter}">
                        <core:Icon id="myAssignedIcon" src="sap-icon://pending" size="1.5rem" color="#e76500" class="sapUiTinyMarginBottom" />
                        <ObjectNumber
                            id="myAssignedCount"
                            number="{assignAnalytics>/assigned}"
                            state="Warning"
                            class="analyticsNumber" />
                        <Text id="myAssignedLabel" text="{i18n>toStart}" class="analyticsLabel" />
                    </VBox>

                    <VBox id="myInProgressBox" alignItems="Center" class="analyticsCard analyticsCardBlue analyticsCardClickable" tooltip="{i18n>clickToFilter}">
                        <core:Icon id="myInProgressIcon" src="sap-icon://activity-2" size="1.5rem" color="#0854a0" class="sapUiTinyMarginBottom" />
                        <ObjectNumber
                            id="myInProgressCount"
                            number="{assignAnalytics>/inProgress}"
                            state="Information"
                            class="analyticsNumber" />
                        <Text id="myInProgressLabel" text="{i18n>inProgress}" class="analyticsLabel" />
                    </VBox>

                    <VBox id="myOverdueBox" alignItems="Center" class="analyticsCard analyticsCardRed analyticsCardClickable" tooltip="{i18n>clickToFilter}">
                        <core:Icon id="myOverdueIcon" src="sap-icon://alert" size="1.5rem" color="#bb0000" class="sapUiTinyMarginBottom" />
                        <ObjectNumber
                            id="myOverdueCount"
                            number="{assignAnalytics>/overdue}"
                            state="Error"
                            class="analyticsNumber" />
                        <Text id="myOverdueLabel" text="{i18n>overdue}" class="analyticsLabel" />
                    </VBox>

                    <VBox id="myCompletedBox" alignItems="Center" class="analyticsCard analyticsCardGreen analyticsCardClickable" tooltip="{i18n>clickToFilter}">
                        <core:Icon id="myCompletedIcon" src="sap-icon://accept" size="1.5rem" color="#107e3e" class="sapUiTinyMarginBottom" />
                        <ObjectNumber
                            id="myCompletedCount"
                            number="{assignAnalytics>/completed}"
                            state="Success"
                            class="analyticsNumber" />
                        <Text id="myCompletedPctText"
                            text="{= ${assignAnalytics>/completionPercent} + '% completed'}"
                            class="analyticsLabel analyticsSubLabel" />
                        <Text id="myCompletedLabel" text="{i18n>completed}" class="analyticsLabel" />
                    </VBox>
                </VBox>
            </Panel>

            <!-- SmartFilterBar for Assignments -->
            <smartFilterBar:SmartFilterBar
                id="assignSmartFilterBar"
                entitySet="TrainingAssignments"
                persistencyKey="AssignmentsSmartFilter"
                ariaLabelledBy="assignmentsFilterLabel"
                enableBasicSearch="true"
                showClearOnFB="false"
                showRestoreOnFB="false"
                showGoOnFB="true"
                filterBarExpanded="true"
                showFilterConfiguration="true"
                useDateRangeType="true"
                search="onFilterBarSearch">
                <smartFilterBar:controlConfiguration>
                    <smartFilterBar:ControlConfiguration id="ccAssignModule" key="SapModule" filterType="single" />
                </smartFilterBar:controlConfiguration>
                <smartFilterBar:filterGroupItems>
                    <filterBar:FilterGroupItem
                        id="fgiAssignStatus"
                        name="Status"
                        label="{i18n>statusLabel}"
                        groupName="_BASIC"
                        visibleInFilterBar="true">
                        <filterBar:control>
                            <Select id="filterAssignStatus" forceSelection="false" selectedKey="">
                                <core:Item id="assignStatusAll" key="" text="{i18n>allItems}" />
                                <core:Item id="assignStatusAssigned" key="Assigned" text="{i18n>statusAssigned}" />
                                <core:Item id="assignStatusInProgress" key="In Progress" text="{i18n>inProgress}" />
                                <core:Item id="assignStatusCompleted" key="Completed" text="{i18n>completed}" />
                                <core:Item id="assignStatusOverdue" key="Overdue" text="{i18n>overdue}" />
                            </Select>
                        </filterBar:control>
                    </filterBar:FilterGroupItem>
                </smartFilterBar:filterGroupItems>
            </smartFilterBar:SmartFilterBar>

            <!-- ====== CARD GRID VIEW (Learning Hub style) ====== -->
            <ScrollContainer
                id="assignCardScrollContainer"
                height="100%"
                width="100%"
                vertical="true"
                horizontal="false"
                visible="{assignViewMode>/showCards}">
                <VBox id="assignCardGridWrapper" class="cardGridWrapper">
                    <!-- Card count + View toggle toolbar -->
                    <Toolbar id="assignCardToolbar" class="sapUiSmallMarginBottom">
                        <Title id="assignCardCountTitle" text="{parts: ['i18n>myAssignmentsCount', 'assignViewMode>/cardCount'], formatter: 'jQuery.sap.formatMessage'}" level="H5" />
                        <ToolbarSpacer id="assignCardToolbarSpacer1" />
                        <SegmentedButton id="assignViewModeToggle" selectedKey="{assignViewMode>/mode}" selectionChange="onAssignViewModeChange">
                            <items>
                                <SegmentedButtonItem id="assignViewModeCards" key="cards" icon="sap-icon://grid" tooltip="{i18n>cardViewTooltip}" />
                                <SegmentedButtonItem id="assignViewModeTable" key="table" icon="sap-icon://table-view" tooltip="{i18n>tableViewTooltip}" />
                            </items>
                        </SegmentedButton>
                        <Button
                            id="assignRefreshBtnCard"
                            icon="sap-icon://refresh"
                            tooltip="{i18n>refreshData}"
                            press="onRefresh" />
                    </Toolbar>
                    <!-- Assignment Card Grid Container -->
                    <f:GridList
                        id="assignCardGrid"
                        mode="None"
                        growing="true"
                        growingThreshold="30"
                        growingScrollToLoad="true"
                        noDataText="{i18n>noAssignmentsCardText}"
                        items="{ path: '/TrainingAssignments', sorter: { path: 'DueDate', descending: false } }">
                        <f:customLayout>
                            <cssgrid:GridBoxLayout
                                boxMinWidth="16rem"
                                boxWidth="24rem" />
                        </f:customLayout>
                        <f:items>
                            <CustomListItem type="Active" press="onAssignCardPress" class="learningCard assignmentCard">
                                <HBox class="learningCardContent">
                                    <VBox class="learningCardBody" width="100%">
                                        <HBox alignItems="Center" class="learningCardTitleRow">
                                            <core:Icon
                                                src="{path: 'SapModule', formatter: '.getModuleIcon'}"
                                                size="1.25rem"
                                                color="{path: 'SapModule', formatter: '.getModuleIconColor'}"
                                                class="sapUiTinyMarginEnd" />
                                            <Text text="{Title}" class="learningCardTitle" maxLines="2" />
                                        </HBox>
                                        <HBox class="learningCardMeta sapUiTinyMarginTop" wrap="Wrap">
                                            <ObjectStatus
                                                text="{parts: [{path:'Status'}, {path:'DueDate'}], formatter: '.formatCardStatus'}"
                                                state="{parts: [{path:'Status'}, {path:'DueDate'}], formatter: '.formatCardStatusState'}"
                                                icon="{parts: [{path:'Status'}, {path:'DueDate'}], formatter: '.formatCardStatusIcon'}"
                                                class="sapUiTinyMarginEnd"
                                                inverted="true" />
                                            <!-- C3: Priority badge -->
                                            <ObjectStatus
                                                text="{Priority}"
                                                state="{path: 'Priority', formatter: '.formatPriorityState'}"
                                                icon="{path: 'Priority', formatter: '.formatPriorityIcon'}"
                                                visible="{= !!${Priority} }"
                                                class="sapUiTinyMarginEnd" />
                                            <ObjectStatus text="{SapModule}" state="None" icon="sap-icon://tree" inverted="false" />
                                        </HBox>
                                        <HBox class="learningCardMeta sapUiTinyMarginTop" wrap="Wrap">
                                            <ObjectStatus
                                                text="{path: 'DueDate', type: 'sap.ui.model.type.Date', formatOptions: {style: 'medium'}}"
                                                state="None"
                                                icon="sap-icon://date-time"
                                                class="sapUiTinyMarginEnd"
                                                inverted="false" />
                                            <ObjectStatus text="{UserName}" state="None" icon="sap-icon://person-placeholder" inverted="false" />
                                        </HBox>
                                    </VBox>
                                    <VBox class="learningCardActions" alignItems="Center" justifyContent="Center">
                                        <Button icon="sap-icon://begin" tooltip="{i18n>startTrainingTooltip}" press="onAssignCardStart" type="Accept" />
                                        <Button icon="sap-icon://complete" tooltip="{i18n>markCompletedTooltip}" press="onAssignCardComplete" type="Emphasized" />
                                        <Button icon="sap-icon://inspect" tooltip="{i18n>viewDetailsTooltip}" press="onAssignCardDetail" type="Transparent" />
                                        <Button icon="sap-icon://action" tooltip="{i18n>openTrainingUrlTooltip}" press="onAssignCardOpenUrl" type="Transparent" />
                                    </VBox>
                                </HBox>
                            </CustomListItem>
                        </f:items>
                    </f:GridList>
                </VBox>
            </ScrollContainer>

            <!-- SmartTable for Assignments -->
            <smartTable:SmartTable
                id="assignSmartTable"
                entitySet="TrainingAssignments"
                smartFilterId="assignSmartFilterBar"
                tableType="ResponsiveTable"
                header="{i18n>trainingAssignments}"
                showRowCount="true"
                enableAutoBinding="true"
                showFullScreenButton="true"
                enableExport="true"
                useVariantManagement="false"
                useTablePersonalisation="true"
                persistencyKey="AssignmentsSmartTableV5"
                demandPopin="true"
                enableAutoColumnWidth="true"
                fitContainer="true"
                initialise="onSmartTableInit"
                initiallyVisibleFields="Title,Url,SapModule,UserId,UserName,Status,DueDate,CompletionDate"
                beforeRebindTable="onBeforeRebindTable"
                requestAtLeastFields="Url,Status,DueDate"
                visible="{assignViewMode>/showTable}">



                <smartTable:customToolbar>
                    <OverflowToolbar id="assignTableToolbar">
                        <Button
                            id="startTrainingBtn"
                            text="{i18n>startTraining}"
                            icon="sap-icon://begin"
                            type="Accept"
                            press="onStartTraining" />
                        <Button
                            id="markCompletedBtn"
                            text="{i18n>markCompleted}"
                            icon="sap-icon://complete"
                            type="Ghost"
                            press="onMarkCompletedBtn"
                            class="markCompletedToolbarBtn" />
                        <ToolbarSpacer id="assignToolbarSpacer" />
                        <SegmentedButton id="assignViewModeToggle2" selectedKey="{assignViewMode>/mode}" selectionChange="onAssignViewModeChange">
                            <items>
                                <SegmentedButtonItem id="assignViewModeCards2" key="cards" icon="sap-icon://grid" tooltip="{i18n>cardViewTooltip}" />
                                <SegmentedButtonItem id="assignViewModeTable2" key="table" icon="sap-icon://table-view" tooltip="{i18n>tableViewTooltip}" />
                            </items>
                        </SegmentedButton>
                        <Button
                            id="assignRefreshBtn"
                            icon="sap-icon://refresh"
                            tooltip="{i18n>refreshData}"
                            ariaLabelledBy="assignRefreshLabel"
                            press="onRefresh" />
                    </OverflowToolbar>
                </smartTable:customToolbar>
            </smartTable:SmartTable>
        </content>
    </Page>
</mvc:View>
