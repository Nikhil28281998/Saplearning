{
	"name": "skillforge-training-platform",
	"version": "1.0.1",
	"repository": "<Add your repository here>",
	"license": "UNLICENSED",
	"description": "SkillForge Training Platform - S4HANA On-Premise",
	"dependencies": {
		"@sap/cds": "^8",
		"@sap/xssec": "^4",
		"express": "^4"
	},
	"devDependencies": {
		"@sap/cds-dk": "^8",
		"bestzip": "^2.2.1",
		"rimraf": "^5.0.0",
		"@sap/ux-specification": "latest"
	},
	"scripts": {
		"start": "cds-serve",
		"build": "cds build --production",
		"deploy": "cd ui/z.sap.courses && npm run deploy"
	},
	"cds": {
		"requires": {
			"db": {
				"kind": "sql",
				"[production]": {
					"kind": "hana"
				}
			},
			"auth": {
				"kind": "dummy",
				"[production]": {
					"kind": "xsuaa"
				}
			}
		}
	}
}